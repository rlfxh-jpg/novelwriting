# 项目 AGENTS 规范

本文件用于约束在当前小说项目内工作的 AI agent 行为。
它只对本项目生效，优先级高于全局 `AGENTS.md`。

## 1. 项目目标

- 本项目是中文长篇幻想/魔法体系小说设定与剧情协作仓库。
- 所有新增内容都应服务于以下目标：
  - 强化世界观一致性
  - 强化术语统一性
  - 强化角色口吻区分度
  - 强化剧情推进效率
  - 保持“魔法文明 = 可被工程化理解的复杂系统”这一核心特色

## 2. 语言与文风

- 默认使用中文输出。
- 写作性内容优先自然、清晰、具体，不写空泛口号。
- 设定文风格应偏“可检索、可引用、可扩展”，避免纯抒情描述。
- 剧情、场景、角色对白要避免机器式平均用语，角色说话方式应有区分。
- 不要无故把文本写成教程腔、产品文档腔或互联网营销文案腔。

## 3. 修改原则

- 先读相关设定，再补充新内容，不得凭空覆盖既有世界观。
- 发现冲突时，优先做以下处理：
  - 先标出冲突点
  - 再给出兼容方案
  - 非必要不直接重写旧设定
- 不得为了“更完整”而无节制扩写无关内容。
- 保留已有文档的命名风格、章节层级和表达习惯。
- 没有明确要求时，不批量改文件名，不重排整库结构。

## 4. 设定一致性要求

- 任何新增设定都要检查是否与以下维度冲突：
  - 世界历史时间线
  - 十大本源与十大家族体系
  - 国家、学院、神殿、遗迹、城市等组织结构
  - 魔法网络、服务、节点、接口、术式、能源体系
  - 主角与核心角色既有经历、动机和关系弧光
- 若引入新术语，优先复用现有词汇体系；确需新增时，名称必须可归类、可解释。
- 同一概念不要在不同文档中反复改名，除非用户明确要求统一重写。

## 5. 剧情与角色要求

- 剧情设计必须兼顾“冲突升级”“信息揭示”“角色选择后果”三件事，不能只补背景。
- 主角相关内容要维持成长逻辑，避免突然全知、突然无敌、突然性格反转。
- 配角必须有明确功能：推动剧情、制造冲突、揭示世界、映射主题，至少占其一。
- 对话要能体现身份、立场、知识层级，不要让所有角色说同一种话。
- 高概念设定最终要能落回人物处境和具体场景。

## 6. 文件协作规则

- 优先在最相关的现有文档里补充，而不是随意新建文件。
- 只有在以下情况才新建文档：
  - 主题已经明显独立
  - 现有文件继续追加会破坏结构
  - 用户明确要求拆分
- 新建文件名要延续当前项目风格，优先使用中文名并直述主题。
- 如果一个改动影响多个文档，最终说明里要指出哪些文件可能需要后续联动校对。

## 7. Memory 日志规则

- 项目根目录下维护 `memory/` 目录，作为对话记忆沉淀目录。
- 记忆文件按日期创建，文件名固定为当天日期，格式为 `YYYY-MM-DD.md`。
- 每次与用户发生一次新的对话交互后，都应向当天的记忆文件追加一条新记忆，而不是覆盖旧内容。
- 单条记忆应尽量简洁，记录本次交流中对后续创作或协作有价值的信息，例如：
  - 用户新增的约束
  - 用户确认的偏好
  - 项目结构调整
  - 新增的重要设定方向
- 若当天记忆文件不存在，则先创建再追加。
- 除非用户明确要求，不要修改或重写过往日期的记忆内容，只允许向当天文件追加。

## 8. 输出要求

- 给设定建议时，优先使用以下结构：
  - 结论
  - 与现有设定的衔接点
  - 可能冲突点
  - 建议写入位置
- 给剧情建议时，优先使用以下结构：
  - 这一段要解决什么问题
  - 冲突如何发生
  - 信息如何揭示
  - 人物关系如何变化
- 用户要求直接写正文时，输出应可直接进入草稿，而不是只给提纲式碎片。

## 9. 禁止事项

- 不要擅自把本项目改写成轻小说、网游说明书、科幻白皮书或纯系统文。
- 不要引入与既有世界观明显不兼容的现代现实梗，除非用户明确要求。
- 不要为了“酷”而堆砌专有名词，导致信息密度过高且不可读。
- 不要把每个设定都解释得过满，保留一部分神秘感和叙事空间。

## 10. 默认完成标准

在本项目内，除非用户只是随口提问，否则完成任务通常意味着：

1. 已读取与任务最相关的现有文档
2. 已确认新增内容与关键设定基本不冲突
3. 已完成修改或写作
4. 已说明本次内容落在哪些文件，以及是否存在后续联动调整点
